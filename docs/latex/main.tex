% !TeX program = lualatex
% ==========================================================
% MAIN.TEX — MANUAL TÉCNICO
% Sistema de Exámenes de Colocación — Centro Boliviano Americano
% Motor: LuaLaTeX + BibTeX
% ==========================================================

\documentclass[12pt,letterpaper,oneside]{report}

% ── CONFIGURACIÓN ─────────────────────────────────────────
\input{config}

% ── IDIOMA Y TIPOGRAFÍA ───────────────────────────────────
\usepackage[english]{babel}
\addto\captionsenglish{%
  \renewcommand{\contentsname}{Índice}%
  \renewcommand{\listfigurename}{Índice de figuras}%
  \renewcommand{\listtablename}{Índice de tablas}%
  \renewcommand{\chaptername}{Capítulo}%
  \renewcommand{\bibname}{Referencias}%
  \renewcommand{\figurename}{Figura}%
  \renewcommand{\tablename}{Tabla}%
}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% ── MÁRGENES ─────────────────────────────────────────────
\usepackage{geometry}
\geometry{top=2.5cm,bottom=2.5cm,left=3cm,right=2.5cm}
\setlength{\footskip}{28pt}
\setlength{\headheight}{43pt}

% ── GRÁFICOS Y TABLAS ────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{array}
\usepackage{caption}
\usepackage{makecell}

% ── COLUMNAS PERSONALIZADAS ───────────────────────────────
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}

\captionsetup{
  labelformat  = simple,
  labelsep     = newline,
  labelfont    = bf,
  textfont     = it,
  font         = small,
  justification= raggedright,
  singlelinecheck = false
}

% ── COLORES ──────────────────────────────────────────────
\usepackage{xcolor}
\usepackage{amssymb}
\definecolor{IHBlue}{HTML}{2563EB}
\definecolor{IHDarkBlue}{HTML}{1E40AF}
\definecolor{IHGray}{HTML}{2B2B2B}
\definecolor{IHLight}{HTML}{EFF6FF}
\definecolor{IHSky}{HTML}{3B82F6}
\definecolor{IHLavender}{HTML}{BFDBFE}
\definecolor{IHAmber}{HTML}{D97706}

% ── LISTAS ───────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*,itemsep=4pt}
\setlist[enumerate]{leftmargin=*,itemsep=4pt}

% ── ENLACES ──────────────────────────────────────────────
\usepackage{hyperref}
\usepackage{xurl}
\hypersetup{colorlinks=true,linkcolor=IHBlue,urlcolor=IHBlue,citecolor=IHBlue,linktoc=all}

% ── CAJAS VISUALES ───────────────────────────────────────
\usepackage[most]{tcolorbox}
\tcbset{enhanced,breakable,colback=white,coltext=IHGray,boxrule=0.8pt,arc=2mm,
        left=2mm,right=2mm,top=2mm,bottom=2mm,fonttitle=\bfseries\sffamily}
\newtcolorbox{IHBoxInfo}[1]{title={#1},colframe=IHBlue,colbacktitle=IHBlue,coltitle=white}
\newtcolorbox{IHBoxWarn}[1]{title={#1},colframe=IHAmber,colbacktitle=IHAmber,coltitle=white}

% ── LISTINGS DE CÓDIGO ───────────────────────────────────
\usepackage{listings}
\usepackage{textcomp}
\lstdefinelanguage{PostgreSQL}{
  keywords={CREATE,ALTER,DROP,TABLE,INDEX,VIEW,FUNCTION,TRIGGER,
    PROCEDURE,INSERT,UPDATE,DELETE,SELECT,FROM,WHERE,INNER,LEFT,
    RIGHT,JOIN,ON,GROUP,BY,ORDER,ASC,DESC,HAVING,LIMIT,OFFSET,
    AS,IN,NOT,NULL,IS,AND,OR,TRUE,FALSE,EXISTS,UNION,ALL,
    DISTINCT,CASE,WHEN,THEN,ELSE,END,BEGIN,DECLARE,RETURN,
    RAISE,EXCEPTION,INTEGER,VARCHAR,TEXT,BOOLEAN,UUID,
    TIMESTAMPTZ,NUMERIC,DEFAULT,CHECK,UNIQUE,PRIMARY,KEY,
    REFERENCES,FOREIGN,CONSTRAINT,ENUM,TYPE,ROW,COUNT,
    LANGUAGE,plpgsql,STABLE,SECURITY,DEFINER,EXECUTE,
    FUNCTION,TRIGGER,NEW,OLD},
  sensitive=false,
  comment=[l]{--},
  morestring=[b]',
  morestring=[b]"
}
\lstdefinelanguage{TypeScript}{
  keywords={export,import,from,interface,type,const,let,var,
    function,return,if,else,for,while,do,switch,case,break,
    default,async,await,new,this,true,false,null,undefined,
    typeof,instanceof,void,never,string,number,boolean,
    Record,Partial,Required,Pick,Omit,Promise},
  sensitive=true,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  morestring=[b]'
}
\lstset{
  basicstyle=\footnotesize\ttfamily,
  frame=single,
  breaklines=true,
  numbers=left,
  numberstyle=\tiny,
  tabsize=2
}

% ── INTERLINEADO ─────────────────────────────────────────
\usepackage{setspace}

% ── ENCABEZADO Y PIE ─────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\setlength{\headsep}{4pt}

\fancyhead[C]{%
  \begin{minipage}{\textwidth}
    \centering
    {\sffamily\bfseries\color{IHBlue}\DocInstitucion\ \textemdash\ \DocFacultad}\\[-1pt]
    {\sffamily\small\color{IHGray}\DocSubtitulo}
  \end{minipage}
}

\fancyfoot[L]{\footnotesize\sffamily \DocCiudad\ \textemdash\ \DocPais\ \quad|\quad \DocAnio\ \quad|\quad \DocTitulo}
\fancyfoot[R]{\sffamily\bfseries\color{IHGray}\thepage}

\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
  \fancyfoot[L]{\footnotesize\sffamily \DocCiudad\ \textemdash\ \DocPais\ \quad|\quad \DocAnio\ \quad|\quad \DocTitulo}
  \fancyfoot[R]{\sffamily\bfseries\color{IHGray}\thepage}
}

% ── TÍTULOS DE CAPÍTULO ───────────────────────────────────
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\sffamily\bfseries\color{IHBlue}}{}{0em}{\Huge}
\titlespacing*{\chapter}{0pt}{-14pt}{10pt}

% ── BIBLIOGRAFÍA ──────────────────────────────────────────
\usepackage[round]{natbib}

% ── ESTABILIDAD ───────────────────────────────────────────
\setlength{\emergencystretch}{3em}
\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% ── INPUT SEGURO ──────────────────────────────────────────
\newcommand{\InputIfExists}[1]{%
  \IfFileExists{#1}{\input{#1}}{%
    \begin{IHBoxWarn}{Archivo pendiente}
      \texttt{\detokenize{#1}} aún no fue creado.
    \end{IHBoxWarn}
}}

% ==========================================================
% DOCUMENTO
% ==========================================================
\begin{document}

% ── PRELIMINARES ──────────────────────────────────────────
\pagenumbering{roman}
\begin{singlespacing}
  \InputIfExists{secciones/00_portada.tex}\clearpage
  \tableofcontents\clearpage
  \listoffigures\clearpage
  \listoftables\clearpage
\end{singlespacing}
\cleardoublepage

% ── CUERPO ────────────────────────────────────────────────
\pagenumbering{arabic}
\onehalfspacing

\chapter{Introducción y Objetivos}
\label{chap:introduccion}
\InputIfExists{secciones/01_introduccion.tex}

\chapter{Arquitectura del Sistema}
\label{chap:arquitectura}
\InputIfExists{secciones/02_arquitectura.tex}

\chapter{Diseño de Base de Datos}
\label{chap:base_datos}
\InputIfExists{secciones/03_base_datos.tex}

\chapter{Seguridad}
\label{chap:seguridad}
\InputIfExists{secciones/04_seguridad.tex}

\chapter{Manual de Usuario}
\label{chap:manual_usuario}
\InputIfExists{secciones/05_manual_usuario.tex}

\chapter{Conclusiones y Recomendaciones}
\label{chap:conclusiones}
\InputIfExists{secciones/06_conclusiones.tex}

% ── REFERENCIAS ───────────────────────────────────────────
\cleardoublepage
\addcontentsline{toc}{chapter}{\bibname}%
\bibliographystyle{plainnat}
\bibliography{referencias}

\end{document}
